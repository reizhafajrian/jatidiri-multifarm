import { IShed, IShedAnimal, IShedDetail } from '@/store/shed'

export const addShedHandler = async (payload: IShed & { uid: string }) => {
  return
}

export const addShedDetailHandler = async (
  payload: IShedDetail & { uid: string }
) => {
  return
}

export const addShedAnimalHandler = async (
  payload: IShedAnimal & { uid: string }
) => {
  return
}
