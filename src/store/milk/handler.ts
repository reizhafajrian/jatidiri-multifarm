import { IMilk } from '@/store/milk'

export const addMilkHandler = async (payload: IMilk & { uid: string }) => {
  return
}

export const editMilkHandler = async (payload: IMilk & { uid: string }) => {
  return
}
